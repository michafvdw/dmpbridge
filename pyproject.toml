[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dmpbridge"
version = "0.1.0"
description = "DMP Bridge: PDF to structured section extraction pipeline"
authors = [
    { name = "Nahid Zeinali" }
]
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "pdfplumber>=0.11",
    "requests>=2.28",
    "Pillow>=11.0",
    "beautifulsoup4>=4.12",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
server = [
    "fastapi>=0.115",
    "uvicorn>=0.34",
    "python-multipart>=0.0.20",
]
notebooks = [
    "matplotlib>=3.10",
    "seaborn>=0.13",
    "pandas>=2.3",
]
dev = [
    "pytest>=8.0",
]
# PDF extractor extras — install the one(s) you want to run experiments with
docling = [
    "docling>=2.0",
]
lighton = [
    "transformers>=4.40",
    "torch>=2.2",
    "pymupdf>=1.24",
]
# Install all extractors at once
all-extractors = [
    "docling>=2.0",
    "transformers>=4.40",
    "torch>=2.2",
    "pymupdf>=1.24",
]

[project.scripts]
dmpbridge              = "dmpbridge.cli.main:main"
dmpbridge-wholedoc     = "dmpbridge.cli.wholedoc_cmd:main"
dmpbridge-evaluate     = "dmpbridge.evaluation.evaluate:main"
dmpbridge-evaluate-new = "dmpbridge.evaluation.annotation_rules:main"
dmpbridge-experiment   = "dmpbridge.evaluation.experiment:main"

[tool.setuptools.packages.find]
include = ["dmpbridge*"]

[tool.ruff]
target-version = "py310"
line-length = 120
exclude = ["venv", "notebooks", "__pycache__", "*.egg-info"]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",  # line too long — prompt strings in classifier.py are intentionally long
]